/* Newsletter aktivieren: anonymisierte FormSubmit-ID statt privater E-Mail verwenden. */
window.TRENDSELECT_CONFIG = Object.freeze({
  newsletterEndpoint: "" // Beispiel: "https://formsubmit.co/ajax/DEINE-FORM-ID"
});
